CTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hamza | 15 | Looking for Friends</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700;800&family=Montserrat:wght@400;600;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background: linear-gradient(45deg, #0a0a0a, #1a1a2e, #16213e);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Poppins', sans-serif;
            color: white;
            padding: 20px;
            position: relative;
            overflow-x: hidden;
        }
        
        /* Animated background */
        .bg-blur {
            position: fixed;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 20% 30%, rgba(66, 133, 244, 0.1) 0%, transparent 50%),
                       radial-gradient(circle at 80% 70%, rgba(219, 68, 55, 0.1) 0%, transparent 50%),
                       radial-gradient(circle at 40% 80%, rgba(244, 180, 0, 0.1) 0%, transparent 50%);
            filter: blur(50px);
            z-index: -1;
        }
        
        .floating-shapes {
            position: fixed;
            width: 100%;
            height: 100%;
            z-index: -1;
        }
        
        .shape {
            position: absolute;
            border-radius: 50%;
            opacity: 0.1;
            animation: float 15s infinite linear;
        }
        
        @keyframes float {
            0% { transform: translateY(0) rotate(0deg); }
            100% { transform: translateY(-100vh) rotate(360deg); }
        }
        
        .main-container {
            width: 100%;
            max-width: 900px;
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
        }
        
        /* Center Profile Card */
        .center-profile {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(15px);
            border-radius: 30px;
            padding: 40px;
            width: 90%;
            max-width: 700px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3),
                        0 0 0 1px rgba(255, 255, 255, 0.05);
            position: relative;
            overflow: hidden;
            margin-bottom: 30px;
        }
        
        .center-profile::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(90deg, #4285f4, #ea4335, #fbbc05, #34a853);
        }
        
        /* Profile Image */
        .profile-img-container {
            width: 180px;
            height: 180px;
            border-radius: 50%;
            margin: 0 auto 25px;
            border: 4px solid rgba(255, 255, 255, 0.2);
            padding: 5px;
            background: linear-gradient(45deg, #4285f4, #ea4335, #fbbc05);
            animation: borderGlow 3s infinite alternate;
            overflow: hidden;
        }
        
        @keyframes borderGlow {
            0% { box-shadow: 0 0 20px rgba(66, 133, 244, 0.5); }
            33% { box-shadow: 0 0 20px rgba(234, 67, 53, 0.5); }
            66% { box-shadow: 0 0 20px rgba(251, 188, 5, 0.5); }
            100% { box-shadow: 0 0 20px rgba(52, 168, 83, 0.5); }
        }
        
        .profile-img {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: #2c3e50;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 70px;
            color: rgba(255, 255, 255, 0.7);
        }
        
        .profile-img img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 50%;
        }
        
        /* Main Text in Center */
        .main-text {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .name {
            font-size: 3.5rem;
            font-weight: 800;
            background: linear-gradient(90deg, #4285f4, #ea4335, #fbbc05);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 10px;
            letter-spacing: 1px;
        }
        
        .age {
            font-size: 1.8rem;
            color: #aaa;
            margin-bottom: 25px;
            font-weight: 300;
        }
        
        .quote {
            font-size: 1.3rem;
            line-height: 1.6;
            color: #ddd;
            max-width: 600px;
            margin: 0 auto 30px;
            padding: 20px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 15px;
            border-left: 4px solid #4285f4;
        }
        
        /* Info Blocks */
        .info-blocks {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            width: 90%;
            max-width: 700px;
        }
        
        .info-block {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 25px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: transform 0.3s, box-shadow 0.3s;
        }
        
        .info-block:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
        }
        
        .block-title {
            font-size: 1.4rem;
            font-weight: 700;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
            color: #4285f4;
        }
        
        .block-title i {
            font-size: 1.2rem;
        }
        
        .tags {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
        }
        
        .tag {
            padding: 10px 18px;
            border-radius: 50px;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s;
        }
        
        .tag:hover {
            transform: scale(1.05);
        }
        
        .like-tag {
            background: rgba(66, 133, 244, 0.15);
            color: #8ab4f8;
            border: 1px solid rgba(66, 133, 244, 0.3);
        }
        
        .dislike-tag {
            background: rgba(234, 67, 53, 0.15);
            color: #f28b82;
            border: 1px solid rgba(234, 67, 53, 0.3);
        }
        
        .personality-tag {
            background: rgba(52, 168, 83, 0.15);
            color: #81c995;
            border: 1px solid rgba(52, 168, 83, 0.3);
        }
        
        /* Bottom CTA */
        .cta {
            margin-top: 40px;
            text-align: center;
            padding: 30px;
            background: rgba(66, 133, 244, 0.1);
            border-radius: 20px;
            width: 90%;
            max-width: 700px;
            border: 1px solid rgba(66, 133, 244, 0.3);
        }
        
        .cta-title {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 20px;
            color: #8ab4f8;
        }
        
        .cta-text {
            font-size: 1.1rem;
            color: #ccc;
            margin-bottom: 25px;
            line-height: 1.6;
        }
        
        .dm-button {
            background: linear-gradient(45deg, #4285f4, #34a853);
            color: white;
            border: none;
            padding: 18px 45px;
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            display: inline-flex;
            align-items: center;
            gap: 12px;
            box-shadow: 0 10px 20px rgba(66, 133, 244, 0.3);
        }
        
        .dm-button:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(66, 133, 244, 0.4);
        }
        
        /* Footer */
        .footer {
            margin-top: 40px;
            text-align: center;
            color: #888;
            font-size: 0.9rem;
            padding: 20px;
            width: 100%;
        }
        
        .heart {
            color: #ea4335;
            animation: heartbeat 1.5s infinite;
        }
        
        @keyframes heartbeat {
            0% { transform: scale(1); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .name {
                font-size: 2.8rem;
            }
            
            .center-profile {
                padding: 30px 20px;
                width: 95%;
            }
            
            .info-blocks {
                grid-template-columns: 1fr;
                width: 95%;
            }
            
            .profile-img-container {
                width: 150px;
                height: 150px;
            }
        }
    </style>
</head>
<body>
    <div class="bg-blur"></div>
    
    <!-- Floating shapes -->
    <div class="floating-shapes" id="shapesContainer"></div>
    
    <div class="main-container">
        <!-- Center Profile Card -->
        <div class="center-profile">
            <div class="profile-img-container">
                <div class="profile-img" id="profileImg">
                    <i class="fas fa-user-circle"></i>
                </div>
            </div>
            
            <div class="main-text">
                <h1 class="name">HAMZA</h1>
                <div class="age">15 YEARS OLD</div>
                
                <div class="quote">
                    "I'm an introvert who often feels down, always looking for online friends. 
                    I don't have friends in real life, so I spend my time gaming, watching anime, 
                    K-dramas, and web series. My DMs are always open for genuine people."
                </div>
            </div>
        </div>
        
        <!-- Info Blocks -->
        <div class="info-blocks">
            <div class="info-block">
                <h3 class="block-title"><i class="fas fa-gamepad"></i> LOVES</h3>
                <div class="tags">
                    <div class="tag like-tag">
                        <i class="fas fa-headset"></i> Gaming
                    </div>
                    <div class="tag like-tag">
                        <i class="fas fa-dragon"></i> Anime
                    </div>
                    <div class="tag like-tag">
                        <i class="fas fa-film"></i> K-Drama
                    </div>
                    <div class="tag like-tag">
                        <i class="fas fa-tv"></i> Web Series
                    </div>
                    <div class="tag like-tag">
                        <i class="fas fa-pizza-slice"></i> Pizza
                    </div>
                    <div class="tag like-tag">
                        <i class="fas fa-music"></i> Music
                    </div>
                </div>
            </div>
            
            <div class="info-block">
                <h3 class="block-title"><i class="fas fa-ban"></i> HATES</h3>
                <div class="tags">
                    <div class="tag dislike-tag">
                        <i class="fas fa-ghost"></i> Ghosting
                    </div>
                    <div class="tag dislike-tag">
                        <i class="fas fa-eye-slash"></i> Being Ignored
                    </div>
                    <div class="tag dislike-tag">
                        <i class="fas fa-comment-slash"></i> Dry Texts
                    </div>
                    <div class="tag dislike-tag">
                        <i class="fas fa-user-slash"></i> Fake People
                    </div>
                    <div class="tag dislike-tag">
                        <i class="fas fa-clock"></i> Being Left on Read
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Bottom CTA -->
        <div class="cta">
            <h2 class="cta-title">LET'S BE FRIENDS!</h2>
            <p class="cta-text">
                I'm looking for genuine connections with people who share similar interests. 
                If you're into gaming, anime, or just need someone to talk to, don't hesitate to reach out.
            </p>
            <button class="dm-button" id="dmButton">
                <i class="fas fa-paper-plane"></i> DM'S ALWAYS OPEN
            </button>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <p>Made with <i class="fas fa-heart heart"></i> by Hamza • Looking for real friends since 2024</p>
        </div>
    </div>

    <script>
        // Create floating shapes
        const shapesContainer = document.getElementById('shapesContainer');
        const colors = ['#4285f4', '#ea4335', '#fbbc05', '#34a853'];
        
        for (let i = 0; i < 15; i++) {
            const shape = document.createElement('div');
            shape.classList.add('shape');
            shape.style.width = `${Math.random() * 100 + 20}px`;
            shape.style.height = shape.style.width;
            shape.style.background = colors[Math.floor(Math.random() * colors.length)];
            shape.style.left = `${Math.random() * 100}%`;
            shape.style.top = `${Math.random() * 100}%`;
            shape.style.opacity = `${Math.random() * 0.1 + 0.05}`;
            shape.style.animationDuration = `${Math.random() * 20 + 15}s`;
            shape.style.animationDelay = `${Math.random() * 5}s`;
            shapesContainer.appendChild(shape);
        }
        
        // DM Button interaction
        const dmButton = document.getElementById('dmButton');
        
        dmButton.addEventListener('click', function() {
            // Button animation
            this.innerHTML = '<i class="fas fa-check"></i> READY TO CHAT!';
            this.style.background = 'linear-gradient(45deg, #34a853, #0d652d)';
            
            // Create message
            const message = document.createElement('div');
            message.style.cssText = `
                position: fixed;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                background: rgba(0, 0, 0, 0.8);
                backdrop-filter: blur(10px);
                color: white;
                padding: 30px;
                border-radius: 20px;
                border: 2px solid #4285f4;
                text-align: center;
                z-index: 1000;
                max-width: 400px;
                width: 90%;
                animation: fadeIn 0.3s ease;
            `;
            message.innerHTML = `
                <h3 style="color: #8ab4f8; margin-bottom: 15px;">🎮 Let's Connect!</h3>
                <p>If you're into:<br>
                • Gaming 🎮<br>
                • Anime 🐉<br>
                • K-Drama 📺<br>
                • Web Series 🎬<br><br>
                Let's be friends! No ghosting, I promise!</p>
                <button id="closeMsg" style="margin-top: 20px; padding: 10px 25px; background: #4285f4; color: white; border: none; border-radius: 10px; cursor: pointer;">
                    OK, Got It!
                </button>
            `;
            document.body.appendChild(message);
            
            // Close button
            document.getElementById('closeMsg').addEventListener('click', function() {
                message.style.animation = 'fadeOut 0.3s ease';
                setTimeout(() => message.remove(), 300);
            });
            
            // Reset button after 3 seconds
            setTimeout(() => {
                dmButton.innerHTML = '<i class="fas fa-paper-plane"></i> DM\'S ALWAYS OPEN';
                dmButton.style.background = 'linear-gradient(45deg, #4285f4, #34a853)';
            }, 3000);
            
            // Add animations if not exist
            if (!document.getElementById('anim-styles')) {
                const style = document.createElement('style');
                style.id = 'anim-styles';
                style.textContent = `
                    @keyframes fadeIn {
                        from { opacity: 0; transform: translate(-50%, -40%); }
                        to { opacity: 1; transform: translate(-50%, -50%); }
                    }
                    @keyframes fadeOut {
                        from { opacity: 1; transform: translate(-50%, -50%); }
                        to { opacity: 0; transform: translate(-50%, -40%); }
                    }
                `;
                document.head.appendChild(style);
            }
        });
        
        // Profile image click
        const profileImg = document.getElementById('profileImg');
        const fileInput = document.createElement('input');
        fileInput.type = 'file';
        fileInput.accept = 'image/*';
        fileInput.style.display = 'none';
        document.body.appendChild(fileInput);
        
        profileImg.addEventListener('click', () => {
            fileInput.click();
        });
        
        fileInput.addEventListener('change', function(event) {
            const file = event.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    profileImg.innerHTML = `<img src="${e.target.result}" alt="Hamza">`;
                    profileImg.querySelector('img').style.borderRadius = '50%';
                };
                reader.readAsDataURL(file);
            }
        });
        
        // Add hover effect to tags
        document.querySelectorAll('.tag').forEach(tag => {
            tag.addEventListener('mouseenter', function() {
                this.style.boxShadow = '0 5px 15px rgba(0, 0, 0, 0.2)';
            });
            
            tag.addEventListener('mouseleave', function() {
                this.style.boxShadow = 'none';
            });
        });
        
        // Console welcome
        console.log('%c👋 Hey there! I\'m Hamza', 'font-size: 18px; color: #4285f4; font-weight: bold;');
        console.log('%c🎮 15 • Gamer • Anime Fan • Looking for Friends', 'font-size: 14px; color: #ea4335;');
        console.log('%c📬 DMs are always open! Let\'s chat!', 'font-size: 14px; color: #34a853;');
    </script>
</body>
</html<!DO>